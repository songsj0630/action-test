name: CI/CD Example

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3.2.0
      - name: Install dependencies
        run: npm install
      - name: Build and test
        run: npm run build
